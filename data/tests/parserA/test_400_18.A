// test_400_18.A
begin
   println !!rW / (!hc == _5a); 
   {
      d2 := _REjP == APc; 
      newline; 
      {
         printch (Uq$M4 <= 57) <= (!b || !9); 
         if (75 <= !29)
            if (99998086 - ((83)))
               newline; 
            else 
               Z6b := (_h) <= (l7B0I2Ldv); 
         else 
            newline; 
         while (!!nPA0l - q)
            while (2 / !(C6FcC / L))
               newline; 
         {
            newline; 
            while (75)
               {
               }
         }
         {
         }
      }
      while (!!_EBF * 9)
         newline; 
      if (!!!007 + 70583)
         println _vd || 623; 
      else 
         if (_7y)
            G := _64d8rY9T; 
         else 
            while (E)
               newline; 
      {
         newline; 
      }
      if (sK9Y8 && 43776)
         newline; 
      else 
         {
         }
      print _TjIBQ12; 
      Kepxll := 1; 
      {
         newline; 
         newline; 
      }
   }
   println !086 || _a; 
   newline; 
   printch _T * 7196; 
   {
      printch (23); 
      newline; 
   }
   printch 59 < 10; 
   printch (8867); 
   {
   }
end