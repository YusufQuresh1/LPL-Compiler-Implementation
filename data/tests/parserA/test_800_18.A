// test_800_18.A
begin
   println _4M && (730 && (!!_W)); 
   if (!_Gtuuqt9 || 0607305)
      if (q - 5071)
         print mWV$pEA <= !W; 
      else 
         zD := _E8vkcs5 < !!(1 - ((H))); 
   else 
      while (!77554 + Al$WyG)
         println ((1044671916) <= (_Yv)) || (379); 
   printch ((((7 <= (3)) <= (_GR < 4996614)) < AKxcwFG) * (!wJ3c - !((!_BX$M - O) && e))) == (ySO7MWJ && Kq); 
   newline; 
   printch 1 < D; 
   print 68 <= _HGG; 
   if (!pB && ((32 && 1301) || 702))
      {
         while (358155 || 673)
            _L := (95 && e3gbi_) / !6; 
         while (4)
            while (_Huf <= (!!74587))
               while (!_qI == 406)
                  if (!_5IhgV && 9)
                     newline; 
                  else 
                     pmU3L := 94; 
         {
            while (!(!8015 || pX) <= 152)
               {
                  newline; 
               }
            {
            }
            newline; 
            println 6; 
            snNj := 65; 
            while (044)
               {
               }
         }
         newline; 
         if (_Ytkn4 == drruiGGQu9xdoa5)
            printch 68 + 3; 
         else 
            while (or)
               newline; 
         while (_t)
            {
            }
         while (769)
            println 77; 
      }
   else 
      {
         println !(!(!!fwz * aaK) || 0) <= 0; 
         printch cRJh < 19243; 
         {
         }
      }
   println RXuzeToOv && 4138633; 
   ij := _Rqtmr8l <= !!!!68; 
   println (!Co * (1)) - (_pj); 
   newline; 
   {
      {
         _IAitr := EFIxZTGpfXC / _v; 
         print 70756; 
         println (HaX7OixFgXt45A_Is20); 
         newline; 
      }
      {
         _SU7AXHI := 22; 
         newline; 
         {
         }
      }
      if (_l)
         {
         }
      else 
         {
            if (81)
               s3YjintkB := 4; 
            else 
               newline; 
            newline; 
            {
            }
         }
   }
   if (jXSGI + 79)
      printch sDV97 || 5; 
   else 
      Af$ := _EM0 / (_$rN); 
   if (_y)
      {
      }
   else 
      {
      }
   {
   }
   printch 13; 
   printch 72 / !79287; 
end